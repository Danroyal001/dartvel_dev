# Miscellaneous
*.class
*.lock
# ...except the Rust lockfile, which pins a native library that ships. The
# blanket *.lock above is aimed at pubspec.lock, and it caught this one by
# accident: h3-quinn 0.0.7 does not compile against current quinn, so the
# working versions are a fact about this crate rather than a preference, and
# an unpinned resolution can pick a broken pairing on a machine that has not
# built before.
#
# It also un-breaks the Rust cache key in .github/workflows/tests.yml, which
# hashes this file and was hashing something not in the repository.
!packages/dartvel_shelf/rust/Cargo.lock
# The same for dartvel_core's crate, and for the same reason twice over:
# tinyvec 1.13.0 does not compile at all ("cannot find macro `vec`"), and an
# unpinned resolution picked it up the day it was published and failed every
# runtime job. The cache key in platform-build-matrix.yml already hashed
# this file, which was not in the repository.
!packages/dartvel_core/rust/Cargo.lock
*.log
*.pyc
*.swp
.buildlog/
.history



# Flutter repo-specific
/bin/cache/
/bin/internal/bootstrap.bat
/bin/internal/bootstrap.sh
/bin/mingit/
/dev/benchmarks/mega_gallery/
/dev/bots/.recipe_deps
/dev/bots/android_tools/
/dev/devicelab/ABresults*.json
/dev/docs/doc/
/dev/docs/flutter.docs.zip
/dev/docs/lib/
/dev/docs/pubspec.yaml
/dev/integration_tests/**/xcuserdata
/dev/integration_tests/**/Pods
/packages/flutter/coverage/
version
analysis_benchmark.json

# packages file containing multi-root paths
.packages.generated

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/generated_plugin_registrant.dart
.packages
.pub-preload-cache/
.pub-cache/
.pub/
.home/
build/
# `build/` is unanchored, so git applies it at every depth. That silently
# excluded packages/dartvel_cli/lib/src/build/ — a source directory, not build
# output — from every commit, and the loss only surfaced when CI cloned the
# repository and `dartvel build` failed to compile for every target. A source
# tree under lib/ is never build output.
!packages/*/lib/**/build/
!packages/*/lib/**/build/**
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# Android related
**/android/**/gradle-wrapper.jar
.gradle/
**/android/captures/
**/android/gradlew
**/android/gradlew.bat
**/android/local.properties
**/android/**/GeneratedPluginRegistrant.java
**/android/key.properties
*.jks

# iOS/XCode related
**/ios/**/*.mode1v3
**/ios/**/*.mode2v3
**/ios/**/*.moved-aside
**/ios/**/*.pbxuser
**/ios/**/*.perspectivev3
**/ios/**/*sync/
**/ios/**/.sconsign.dblite
**/ios/**/.tags*
**/ios/**/.vagrant/
**/ios/**/DerivedData/
**/ios/**/Icon?
**/ios/**/Pods/
**/ios/**/.symlinks/
**/ios/**/profile
**/ios/**/xcuserdata
**/ios/.generated/
**/ios/Flutter/.last_build_id
**/ios/Flutter/App.framework
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/ephemeral
**/ios/Flutter/app.flx
**/ios/Flutter/app.zip
**/ios/Flutter/flutter_assets/
**/ios/Flutter/flutter_export_environment.sh
**/ios/ServiceDefinitions.json
**/ios/Runner/GeneratedPluginRegistrant.*

# macOS
**/Flutter/ephemeral/
**/Pods/
**/macos/Flutter/GeneratedPluginRegistrant.swift
**/macos/Flutter/ephemeral
**/xcuserdata/

# Windows
**/windows/flutter/generated_plugin_registrant.cc
**/windows/flutter/generated_plugin_registrant.h
**/windows/flutter/generated_plugins.cmake

# Linux
**/linux/flutter/generated_plugin_registrant.cc
**/linux/flutter/generated_plugin_registrant.h
**/linux/flutter/generated_plugins.cmake

# Coverage
coverage/

# Symbols
app.*.symbols

# Exceptions to above rules.
!**/ios/**/default.mode1v3
!**/ios/**/default.mode2v3
!**/ios/**/default.pbxuser
!**/ios/**/default.perspectivev3
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages
!/dev/ci/**/Gemfile.lock

# Rust build artifacts
packages/*/rust/target/

# Example app generated platform folders (generated by dartvel init)
examples/*/android/
examples/*/ios/
examples/*/linux/
examples/*/macos/
examples/*/windows/
examples/*/web/icons/
examples/*/tizen/

# Local-only working directory for unrelated projects that share this
# workspace. Everything inside is excluded, so nothing beyond Dartvel can be
# published by a stray `git add -A`, a fresh clone, or a different machine —
# and the ignore rule itself does not disclose what is in there.
/.private/

# AI working artifacts: context dumps and repo-packing output, not source.
/chat.combined.md
/repomix/
/lib/dartvel_client/
/.dartvel/
/.dart_tool/dartvel_backend.g.dart
/.dart_tool/dartvel_backend_routes.g.dart

# Accidentally committed caches: a pub package cache and an analyzer/home
# directory. Neither is source; both bloated clones by ~535MB.
/.pub-cache/
/.home/
sites/dartvel_site/build/
sites/*.zip

# Client material never belongs in this repository. /.private/ above covers the
# working directories; these cover an archive or capture dropped at the root,
# which is outside it and was how an ImmunityBio zip reached a public remote.
*.zip
*.tar.gz
/immunitybio*
/footlocker*
/premise*

# Agent worktrees. Created under .claude/worktrees when an agent works in an
# isolated copy of the repository, and left behind when it finishes. Ignored
# rather than tidied up by hand: this is a public repository, and a stray
# `git add -A` at the root would commit somebody's whole working copy into it.
.claude/worktrees/
