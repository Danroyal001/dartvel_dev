[package]
name = "dartvel_shelf_core"
version = "0.3.0"
edition = "2021"

[lib]
name = "dartvel_shelf"
crate-type = ["cdylib"]

[dependencies]
axum = { version = "0.7", features = ["http2"] }
# tls-rustls-no-provider, not tls-rustls: the latter drags in aws-lc-rs as
# rustls's default crypto provider. We use ring, so aws-lc was compiled for
# nothing — a large C codebase that also fails on macOS, where cc-rs invokes
# clang without an -isysroot and cannot find stdlib.h.
axum-server = { version = "0.7", default-features = false, features = ["tls-rustls-no-provider"] }
tower = { version = "0.4", features = ["util"] }
tower-http = { version = "0.5", features = ["cors", "compression-full", "fs", "catch-panic"] }
# Already in the graph through axum-server. Named here for the timer: without
# one hyper has no header read timeout, and a connection that sends half a
# request line holds its slot for as long as the client likes.
hyper-util = { version = "0.1", features = ["tokio", "server-auto"] }
bytes = "1"
once_cell = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net", "io-util"] }
num_cpus = "1"
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
rustls-pemfile = "2"
futures-util = "0.3"
tokio-stream = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"

[build-dependencies]
cbindgen = "0.29"

[dev-dependencies]
# The client tests drive async paths directly.
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net", "io-util"] }
